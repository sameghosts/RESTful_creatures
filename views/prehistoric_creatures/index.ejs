<h3>We're BACK! Prehistoric Creature Time!</h3>
<h4>Who's coming to the Dino Party?</h4>
<ul>
  <% preCreats.forEach(preCreat => { %>
  <li>
    <p><strong><%= preCreat.type %></strong> looks like </p>
    <img src="<%= preCreat.img_url %>">
  </li>
  <% }) %>
</ul>